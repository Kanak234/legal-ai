[pytest]
# The offline bundle in backend/venv_packages ships third-party test suites
# (greenlet, etc.). Without this, pytest collects them as if they were this
# project's tests and the run aborts during collection.
testpaths = tests
norecursedirs = backend/venv_packages *.egg .git .tox build dist node_modules
